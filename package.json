{
  "name": "flyrank-auth-assignment",
  "version": "1.0.0",
  "description": "Secure Express API with Supabase Auth: sign up, log in, log out, JWT-verified protected routes, and Swagger UI with bearer auth.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test \"tests/**/*.test.js\""
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "@supabase/supabase-js": "^2.53.0",
    "dotenv": "^17.2.1",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
